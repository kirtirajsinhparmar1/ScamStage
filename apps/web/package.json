{
  "name": "scamstage-web",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "start": "python3 -m http.server 3000 --bind 127.0.0.1",
    "check": "node --check src/api.js && node --check src/audio.js && node --check src/app.js"
  },
  "engines": { "node": ">=18" }
}
