# Frontend — Person 1
Build the Next.js UI here. No frontend runtime is installed yet.
Start with `packages/contracts/fixtures/turn-result.json` and `participant-turn.json`.
Backend: http://localhost:8000; health: GET /api/health.
No session or WebSocket endpoint exists yet. Render null audio as text-only.
Keep provider keys on the backend. Coordinate contract changes with Person 2.
