# Copy to .env at the repository root. No API keys are needed for this milestone.
APP_NAME=SCAMSTAGE
USE_MOCK_CLASSIFIER=true
USE_MOCK_VOICE=true
CORS_ORIGINS=["http://localhost:3000"]
# Future backend-only credentials; not used yet:
# DATABASE_URL=postgresql+psycopg://scamstage:local-only@db:5432/scamstage
# NVIDIA_API_KEY=
# ELEVENLABS_API_KEY=
